@page
@model ProjetoAssembly_Final.Pages.PerfilEditModel
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<ProjetoAssembly_Final.Pages.PerfilEditModel> Localizer
@{
    Layout = "_Layout";    
    ViewData["Title"] = Localizer["Configurações de Perfil"];
}

@section Styles {
    <link rel="stylesheet" href="~/css/pages/perfilEdit.css" asp-append-version="true"/>
}

<div class="profile-edit-wrapper">
    <div class="edit-header-top">
        <h2><i class="fa-solid fa-user-gear"></i> @Localizer["Configurações da Conta"]</h2>
        <p>@Localizer["Gere as tuas informações pessoais e preferências."]</p>
    </div>

    <div class="edit-card">
        <form method="post" enctype="multipart/form-data">
            <input type="hidden" asp-for="CurrentUser.UserId" />
            <input type="hidden" asp-for="CurrentUser.UserName" />

            <div class="edit-grid-layout">
                <div class="photo-edit-section">
                    <div class="avatar-wrapper">
                        <img src="@(Model.CurrentUser?.ProfilePicture != null && Model.CurrentUser.ProfilePicture.StartsWith("http")
                                          ? Model.CurrentUser.ProfilePicture
                                          : "/img/user.icon.png")"
                             id="previewImg" class="profile-edit-img" />

                        <label for="foto" class="btn-change-photo">
                            <i class="fa-solid fa-camera"></i>
                        </label>
                        <input type="file" id="foto" asp-for="PhotoUpload" style="display:none;" accept="image/*" onchange="previewFile()" />
                    </div>
                    <p class="photo-hint">@Localizer["Alterar foto de perfil"]</p>
                </div>

                <div class="inputs-sections">
                    <div class="edit-form-group">
                        <label>@Localizer["Nome de Utilizador"]</label>
                        <input asp-for="InputUserName" class="edit-input" placeholder="@Localizer["Teu nome"]" />
                        <span asp-validation-for="InputUserName" class="text-danger"></span>
                    </div>

                    <div class="edit-form-group">
                        <label>@Localizer["E-mail"]</label>
                        <input asp-for="InputEmail" class="edit-input" />
                        <span asp-validation-for="InputEmail" class="text-danger"></span>
                    </div>

                    <div class="edit-actions">
                        <button type="submit" class="btn-save">
                            <i class="fa-solid fa-floppy-disk"></i> @Localizer["Guardar Alterações"]
                        </button>
                        <a asp-page="/perfil" class="btn-back">@Localizer["Cancelar"]</a>
                    </div>
                </div>
            </div>
        </form>
    </div>    
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" /> @* Para validação no lado do cliente *@
    <script>
        function previewFile() {
            const preview = document.querySelector('#previewImg');
            const file = document.querySelector('#foto').files[0];
            const reader = new FileReader();

            reader.onloadend = function () {
                preview.src = reader.result;
            }

            if (file) {
                reader.readAsDataURL(file);
            }
        }
    </script>
}
